{
	"name": "tests",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"server-run:dev-app": "npm run dev --workspace vite-dev-app",
		"server-run:prod-app": "npm run start --workspace vite-prod-app",
		"server": "run-p server-run:*",
		"test-run:cli-features": "npm run test:cli-features --workspace sahne-app",
		"test-run:api-features": "npm run test --workspace sahne-app",
		"pretest": "npm run server &",
		"test": "run-p test-run:*",
		"posttest": "npm run kill-servers",
		"kill-servers": "npx kill-port 5173 && npx kill-port 8080"
	},
	"workspaces": [
		"sahne-app",
		"vite-dev-app",
		"vite-prod-app"
	],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"npm-run-all": "^4.1.5"
	}
}
