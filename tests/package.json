{
	"name": "tests",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"server:dev-app": "npm run dev --workspace vite-dev-app",
		"server:prod-app": "npm run start --workspace vite-prod-app",
    "start:servers": "run-p server:*",
		"run:sahne-test": "npm run test --workspace sahne",
		"run:test": "run-p start:servers run:sahne-test"
	},
	"workspaces": [
		"sahne",
		"vite-dev-app",
		"vite-prod-app"
	],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"npm-run-all": "^4.1.5"
	}
}
