{
	"name": "tests",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"server:dev-app": "npm run dev --workspace vite-dev-app",
		"server:prod-app": "npm run start --workspace vite-prod-app",
		"test:cli-features": "npm run test:cli-features --workspace sahne",
		"test:api-features": "npm run test --workspace sahne",
		"test": "run-p server:* test:*",
		"test:ci": "run-p server:* & npm-p test:*",
		"kill-servers": "npx kill-port 5173 && npx kill-port 8080"
	},
	"workspaces": [
		"sahne",
		"vite-dev-app",
		"vite-prod-app"
	],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"npm-run-all": "^4.1.5"
	}
}
