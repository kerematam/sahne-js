# Sahne Vite Playground

TypeScript playground for running Sahne against a Vite + React application.

```bash
npm install
npm run build
npm run preview
```

Run the Vite development server separately, then launch Sahne from this directory:

```bash
npm run dev
PUPPETEER_EXECUTABLE_PATH="/path/to/chrome" npm run sahne
```
